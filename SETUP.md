# 🚀 Инструкция по настройке PopFlix

## 1. Настройка Supabase

### Шаг 1: Создание проекта
1. Перейдите на https://supabase.com
2. Создайте новый проект
3. Запишите URL проекта и anon key

### Шаг 2: Настройка базы данных
1. Откройте SQL Editor в Supabase Dashboard
2. Скопируйте содержимое файла `supabase-schema.sql`
3. Вставьте в SQL Editor и выполните (Run)
4. Убедитесь, что все таблицы созданы без ошибок

### Шаг 3: Настройка аутентификации
1. В Supabase Dashboard перейдите в Authentication → Settings
2. Включите Email provider
3. Настройте Email templates (опционально)

## 2. Настройка TMDB API

### Шаг 1: Получение API ключа
1. Перейдите на https://www.themoviedb.org
2. Зарегистрируйтесь или войдите
3. Перейдите в Settings → API
4. Запросите API ключ (выберите Developer)
5. Заполните форму (можно указать учебный проект)
6. Скопируйте API Key (v3 auth) и API Read Access Token (v4 auth)

## 3. Настройка переменных окружения

Откройте файл `.env.local` и заполните:

```env
# Supabase (из шага 1)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# TMDB API (из шага 2)
TMDB_API_KEY=your-tmdb-api-key-here
TMDB_API_READ_ACCESS_TOKEN=your-tmdb-read-access-token-here

# Next.js (сгенерируйте случайную строку)
NEXTAUTH_SECRET=your-random-secret-here
NEXTAUTH_URL=http://localhost:3000
```

Для генерации NEXTAUTH_SECRET выполните:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

## 4. Установка зависимостей и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

Откройте http://localhost:3000 в браузере.

## 5. Проверка работы

1. Попробуйте зарегистрироваться
2. Пройдите онбординг (выбор предпочтений)
3. Проверьте главную страницу с фильмами
4. Попробуйте добавить фильм в избранное

## Возможные проблемы

### Ошибка подключения к Supabase
- Проверьте правильность URL и ключей в .env.local
- Убедитесь, что RLS политики настроены

### Ошибка TMDB API
- Проверьте правильность API ключа
- Убедитесь, что не превышен лимит запросов (40 запросов в 10 секунд)

### Ошибка при регистрации
- Проверьте, что таблица profiles создана
- Проверьте, что триггер handle_new_user работает
- Проверьте логи в Supabase Dashboard → Logs

## Полезные ссылки

- [Supabase Documentation](https://supabase.com/docs)
- [TMDB API Documentation](https://developers.themoviedb.org/3)
- [Next.js Documentation](https://nextjs.org/docs)
