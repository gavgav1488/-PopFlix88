# 🚀 Инструкция по пушу в GitHub

## Проблема
Ошибка: `Permission denied` при попытке `git push`

## Решение

### Вариант 1: Использовать Personal Access Token (Рекомендуется)

1. **Создайте Personal Access Token на GitHub:**
   - Перейдите на https://github.com/settings/tokens
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Выберите срок действия и права доступа (минимум: `repo`)
   - Скопируйте созданный токен (он больше не будет показан!)

2. **Обновите remote URL с токеном:**
   ```bash
   git remote set-url origin https://YOUR_TOKEN@github.com/gavgav1488/-PopFlix88.git
   ```
   
   Замените `YOUR_TOKEN` на ваш токен.

3. **Запушьте изменения:**
   ```bash
   git push origin main
   ```

### Вариант 2: Использовать SSH ключ

1. **Сгенерируйте SSH ключ:**
   ```bash
   ssh-keygen -t ed25519 -C "gavgav1488@example.com"
   ```
   Нажмите Enter для всех вопросов (используйте значения по умолчанию).

2. **Скопируйте публичный ключ:**
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

3. **Добавьте ключ на GitHub:**
   - Перейдите на https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Вставьте скопированный ключ
   - Нажмите "Add SSH key"

4. **Измените remote URL на SSH:**
   ```bash
   git remote set-url origin git@github.com:gavgav1488/-PopFlix88.git
   ```

5. **Запушьте изменения:**
   ```bash
   git push origin main
   ```

### Вариант 3: Использовать GitHub Desktop

1. Скачайте и установите [GitHub Desktop](https://desktop.github.com/)
2. Войдите в свой аккаунт GitHub
3. Добавьте локальный репозиторий
4. Нажмите "Push origin"

## Текущее состояние

✅ Все изменения закоммичены локально:
- Детальная страница фильма
- Страницы списков (просмотренные, избранное)
- Поиск фильмов
- Статистика просмотров
- Страница настройки
- Документация (README.md, SETUP.md)

⏳ Ожидают пуша на GitHub

## Быстрая команда (после настройки токена)

```bash
git push origin main
```

## Проверка успешного пуша

После успешного пуша проверьте репозиторий:
https://github.com/gavgav1488/-PopFlix88

Вы должны увидеть все новые файлы и коммиты.
