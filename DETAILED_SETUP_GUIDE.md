# 📖 Подробная инструкция по настройке PopFlix

## 🎯 Что нужно сделать

Для работы приложения нужно настроить 2 сервиса:
1. **Supabase** - база данных и аутентификация (бесплатно)
2. **TMDB API** - данные о фильмах (бесплатно)

Время настройки: ~10-15 минут

---

## 📝 Часть 1: Настройка Supabase (База данных)

### Шаг 1.1: Регистрация на Supabase

1. Открой браузер и перейди на https://supabase.com
2. Нажми кнопку **"Start your project"** (зелёная кнопка справа вверху)
3. Выбери способ входа:
   - **GitHub** (рекомендуется) - нажми "Continue with GitHub"
   - Или используй email

4. Если выбрал GitHub:
   - Авторизуй Supabase в своём GitHub аккаунте
   - Нажми "Authorize supabase"

### Шаг 1.2: Создание проекта

1. После входа ты увидишь Dashboard
2. Нажми **"New project"** (зелёная кнопка)
3. Заполни форму:
   - **Name**: `popflix` (или любое другое имя)
   - **Database Password**: придумай надёжный пароль (СОХРАНИ ЕГО!)
   - **Region**: выбери ближайший регион (например, `Europe (Frankfurt)`)
   - **Pricing Plan**: оставь **Free** (бесплатный)
4. Нажми **"Create new project"**
5. Подожди 1-2 минуты, пока проект создаётся (увидишь индикатор загрузки)

### Шаг 1.3: Получение ключей доступа

1. Когда проект создан, ты окажешься на главной странице проекта
2. В левом меню найди и нажми на иконку **⚙️ Settings** (внизу)
3. В открывшемся меню выбери **"API"**
4. Ты увидишь раздел **"Project API keys"**
5. Найди и скопируй два значения:

   **Project URL** (выглядит примерно так):
   ```
   https://abcdefghijklmnop.supabase.co
   ```
   
   **anon public** (длинная строка, начинается с `eyJ...`):
   ```
   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYzODM2NzIwMCwiZXhwIjoxOTUzOTQzMjAwfQ.abcdefghijklmnopqrstuvwxyz123456789
   ```

6. **ВАЖНО**: Сохрани эти значения в блокнот - они понадобятся позже!

### Шаг 1.4: Создание таблиц в базе данных

1. В левом меню нажми на иконку **🗄️ SQL Editor**
2. Нажми **"New query"** (кнопка справа вверху)
3. Открой файл `supabase-schema.sql` в твоём проекте (он в корне папки)
4. Скопируй **ВСЁ** содержимое файла (Ctrl+A, Ctrl+C)
5. Вставь в SQL Editor в Supabase (Ctrl+V)
6. Нажми **"Run"** (зелёная кнопка справа внизу) или нажми **F5**
7. Подожди несколько секунд
8. Если всё прошло успешно, увидишь сообщение **"Success. No rows returned"**
9. Если есть ошибки - напиши мне, разберёмся

### Шаг 1.5: Проверка создания таблиц

1. В левом меню нажми на иконку **📊 Table Editor**
2. Ты должен увидеть созданные таблицы:
   - `profiles`
   - `user_preferences`
   - `watched_movies`
   - `favorite_movies`
   - `movies_cache`
3. Если видишь эти таблицы - отлично, база данных настроена! ✅

---

## 🎬 Часть 2: Настройка TMDB API (Данные о фильмах)

### Шаг 2.1: Регистрация на TMDB

1. Открой https://www.themoviedb.org
2. Нажми на иконку профиля справа вверху
3. Выбери **"Sign Up"** (Регистрация)
4. Заполни форму:
   - **Username**: придумай имя пользователя
   - **Password**: придумай пароль
   - **Email**: укажи свой email
5. Согласись с условиями использования
6. Нажми **"Sign Up"**
7. Проверь email и подтверди регистрацию (перейди по ссылке из письма)

### Шаг 2.2: Получение API ключа

1. Войди на сайт TMDB (если ещё не вошёл)
2. Нажми на иконку профиля справа вверху
3. Выбери **"Settings"** (Настройки)
4. В левом меню выбери **"API"**
5. Нажми **"Request an API Key"** (Запросить API ключ)
6. Выбери **"Developer"** (для разработчиков)
7. Прочитай и согласись с условиями (поставь галочку)
8. Нажми **"Accept"**

### Шаг 2.3: Заполнение формы заявки

Заполни форму (можно на английском):

1. **Type of Use**: выбери **"Education"** (Образование)
2. **Application Name**: напиши `PopFlix` или `Movie Recommendation App`
3. **Application URL**: можешь написать `http://localhost:3000` или `https://github.com/gavgav1488/-PopFlix88`
4. **Application Summary**: напиши что-то вроде:
   ```
   This is a school project for learning web development. 
   The application provides personalized movie recommendations 
   based on user preferences using TMDB data.
   ```
   (Это школьный проект для изучения веб-разработки. 
   Приложение предоставляет персонализированные рекомендации 
   фильмов на основе предпочтений пользователя, используя данные TMDB.)

5. Нажми **"Submit"** (Отправить)

### Шаг 2.4: Получение ключа

1. После отправки формы ты сразу увидишь свой API ключ
2. Скопируй **API Key (v3 auth)** - это длинная строка из букв и цифр:
   ```
   1234567890abcdef1234567890abcdef
   ```
3. Также скопируй **API Read Access Token (v4 auth)** - ещё более длинная строка:
   ```
   eyJhbGciOiJIUzI1NiJ9.eyJhdWQiOiIxMjM0NTY3ODkwYWJjZGVmMTIzNDU2Nzg5MGFiY2RlZiIsInN1YiI6IjYxYjNjNGU1ZjJhMzY2MDA0MjA4ZjQwZCIsInNjb3BlcyI6WyJhcGlfcmVhZCJdLCJ2ZXJzaW9uIjoxfQ.abcdefghijklmnopqrstuvwxyz123456789
   ```
4. **ВАЖНО**: Сохрани оба значения в блокнот!

---

## ⚙️ Часть 3: Настройка переменных окружения

### Шаг 3.1: Открытие файла .env.local

1. Открой проект PopFlix в редакторе кода (VS Code, Cursor и т.д.)
2. Найди файл `.env.local` в корне проекта
3. Если не видишь файл - включи отображение скрытых файлов
4. Открой файл `.env.local`

### Шаг 3.2: Заполнение переменных

Замени placeholder значения на реальные:

```env
# Supabase (вставь значения из Шага 1.3)
NEXT_PUBLIC_SUPABASE_URL=https://твой-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=твой-anon-ключ-начинается-с-eyJ

# TMDB API (вставь значения из Шага 2.4)
TMDB_API_KEY=твой-api-ключ-32-символа
TMDB_API_READ_ACCESS_TOKEN=твой-токен-начинается-с-eyJ

# Next.js (оставь как есть)
NEXTAUTH_SECRET=можешь-оставить-любую-строку-или-сгенерировать
NEXTAUTH_URL=http://localhost:3000
```

### Шаг 3.3: Генерация NEXTAUTH_SECRET (опционально)

Если хочешь сгенерировать случайный секрет:

1. Открой терминал в папке проекта
2. Выполни команду:
   ```bash
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```
3. Скопируй полученную строку и вставь в `NEXTAUTH_SECRET`

Или просто оставь любую случайную строку (для разработки подойдёт).

### Шаг 3.4: Сохранение файла

1. Сохрани файл `.env.local` (Ctrl+S)
2. **ВАЖНО**: Убедись, что файл сохранён!

---

## 🚀 Часть 4: Запуск приложения

### Шаг 4.1: Остановка старого сервера (если запущен)

1. Если у тебя уже запущен dev сервер, останови его:
   - Нажми **Ctrl+C** в терминале
   - Или закрой терминал

### Шаг 4.2: Запуск нового сервера

1. Открой терминал в папке проекта
2. Выполни команду:
   ```bash
   npm run dev
   ```
3. Подожди несколько секунд
4. Ты должен увидеть:
   ```
   ▲ Next.js 16.1.6
   - Local:        http://localhost:3000
   - Network:      http://10.10.10.107:3000
   
   ✓ Ready in 2s
   ```

### Шаг 4.3: Открытие приложения

1. Открой браузер
2. Перейди на http://localhost:3000
3. Если всё настроено правильно, ты увидишь главную страницу PopFlix! 🎉

---

## ✅ Проверка работы

### Тест 1: Регистрация

1. На главной странице нажми **"Начать бесплатно"**
2. Заполни форму регистрации:
   - Email: любой email (можно тестовый)
   - Пароль: минимум 6 символов
3. Нажми **"Зарегистрироваться"**
4. Если регистрация прошла успешно - ты увидишь страницу онбординга ✅

### Тест 2: Выбор предпочтений

1. На странице онбординга выбери несколько жанров (например, "Боевик", "Комедия")
2. Нажми **"Продолжить"**
3. Ты должен попасть на главную страницу с фильмами ✅

### Тест 3: Просмотр фильмов

1. На главной странице ты должен видеть карточки фильмов с постерами
2. Нажми на любой фильм
3. Откроется детальная страница с информацией о фильме ✅

---

## 🆘 Что делать, если что-то не работает

### Ошибка: "Invalid supabaseUrl"

**Причина**: Неправильно указан URL Supabase в `.env.local`

**Решение**:
1. Проверь, что URL начинается с `https://`
2. Проверь, что URL заканчивается на `.supabase.co`
3. Убедись, что нет лишних пробелов
4. Перезапусти сервер (Ctrl+C, затем `npm run dev`)

### Ошибка: "TMDB API error"

**Причина**: Неправильный API ключ TMDB

**Решение**:
1. Проверь, что ключ скопирован полностью (32 символа)
2. Убедись, что нет лишних пробелов
3. Проверь, что ключ активен на TMDB
4. Перезапусти сервер

### Ошибка: "Failed to fetch"

**Причина**: Проблемы с сетью или сервер не запущен

**Решение**:
1. Убедись, что dev сервер запущен (`npm run dev`)
2. Проверь, что открываешь http://localhost:3000 (не https)
3. Попробуй перезапустить сервер

### Не видно фильмов на главной странице

**Причина**: Проблема с TMDB API

**Решение**:
1. Открой консоль браузера (F12)
2. Посмотри на ошибки в консоли
3. Проверь TMDB API ключ в `.env.local`
4. Убедись, что есть интернет

### Не работает регистрация

**Причина**: Проблема с Supabase

**Решение**:
1. Проверь Supabase URL и ключ в `.env.local`
2. Убедись, что таблицы созданы (Шаг 1.5)
3. Проверь, что триггер `handle_new_user` создан
4. Посмотри логи в Supabase Dashboard → Logs

---

## 📞 Нужна помощь?

Если что-то не получается:

1. Проверь все шаги ещё раз
2. Убедись, что все значения в `.env.local` правильные
3. Перезапусти сервер
4. Очисти кэш браузера (Ctrl+Shift+Delete)
5. Напиши мне - разберёмся вместе!

---

## 🎉 Готово!

Если всё работает - поздравляю! Теперь у тебя есть полностью рабочий онлайн-кинотеатр с:
- ✅ Регистрацией и входом
- ✅ Персональными рекомендациями
- ✅ Поиском фильмов
- ✅ Списками просмотренных и избранного
- ✅ Статистикой просмотров

Приятного использования! 🍿
